langc {
extern "C" {
    int add(int a, int b) {
        return a + b;
    }
}
}

langtsx {
const result = native.add ? native.add(21, 21) : -1;
const el = document.createElement('div');
el.textContent = `native.add(21, 21) = ${result}`;
document.body.appendChild(el);
}
