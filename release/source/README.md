# JELLL (Source Code)

This folder contains the source code for the JELLL compiler.

## Prerequisites for building

* A C++17 compatible compiler (MSVC `cl.exe` or MinGW `g++`)

## Build Instructions

Simply run the included build script from a developer command prompt:

```bash
build_jelll.bat
```

This will produce `jelll.exe` in the current directory.
