# JEBC (Source Code)

This folder contains the source code for the JEBC compiler.

## Prerequisites for building

* A C++20 compatible compiler (MSVC `cl.exe` or MinGW `g++`)

## Build Instructions

Simply run the included build script from a developer command prompt:

```bash
build_jebc.bat
```

This will produce `jebc.exe` in the current directory.
